A software engineer who installs CodeGraph and indexes their repository. At the container level they enter the system through the CLI.

## Responsibilities
- Invokes the `codegraph` CLI to install, initialize, index, sync, and query
- Owns the machine and the `.codegraph/` directory the index is written to

## Tech Stack
- Terminal; the `codegraph` binary
